// CustomCursor removed — kept file as a harmless stub to avoid import errors during runtime while
// ensuring no custom cursor behavior remains in the app. The component renders nothing.

export default function CustomCursor() {
  return null;
}
